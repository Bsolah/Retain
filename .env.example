# Shared local development defaults
# Copy service-specific examples into each app/package as needed:
#   cp apps/api/.env.example apps/api/.env
#   cp apps/ai/.env.example apps/ai/.env
#   cp apps/admin/.env.example apps/admin/.env
#   cp apps/portal/.env.example apps/portal/.env
#   cp apps/webhook-worker/.env.example apps/webhook-worker/.env
#   cp packages/database/.env.example packages/database/.env

NODE_ENV=development

DATABASE_URL=postgresql://retain:retain@localhost:5433/retain
REDIS_URL=redis://localhost:6380
CLICKHOUSE_URL=http://retain:retain@localhost:8123/retain
