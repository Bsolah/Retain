apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "retain-worker.fullname" . }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      app.kubernetes.io/name: retain-webhook-worker
