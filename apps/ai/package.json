{
  "name": "@retain/ai",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "./.venv/bin/python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "./.venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "build": "./.venv/bin/python -m compileall app && mkdir -p dist && touch dist/.build",
    "lint": "./.venv/bin/python -m ruff check app",
    "typecheck": "./.venv/bin/python -c \"from app.main import app; assert app.title == 'Retain AI'\"",
    "test": "./.venv/bin/python -m pytest tests -q"
  }
}
