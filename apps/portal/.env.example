# Public API origin (must match the host that sets OAuth cookies).
# Local API only works if OAuth callback also runs on localhost.
# With Cloudflare tunnels, use the API tunnel URL.
VITE_API_URL=http://localhost:3001
VITE_PORTAL_URL=http://localhost:5174
